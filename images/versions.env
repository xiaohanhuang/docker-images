# ============================================================
# Central version pins for all ml-platform container images.
# Every Makefile target and Dockerfile ARG reads from here.
#
# To release a new set of images, bump IMAGE_TAG and rebuild.
# Run `make build-all` to build the full layer chain in order.
# ============================================================

# ── Upstream base versions ──────────────────────────────────
CUDA_VERSION   := 12.9.1
PYTHON_VERSION := 3.12

# ── Framework versions ──────────────────────────────────────
TORCH_VERSION      := 2.10.0
TORCHVISION_VERSION:= 0.25.0
TORCHAUDIO_VERSION := 2.10.0
FLYTEKIT_VERSION   := 1.16.14
RAY_VERSION        := 2.54.0

# ── Data workload versions ──────────────────────────────────
PANDAS_VERSION      := 2.2.2
PYARROW_VERSION     := 23.0.1
BOTO3_VERSION       := 1.42.61
S3FS_VERSION        := 2026.2.0
SKLEARN_VERSION     := 1.8.0
TIKTOKEN_VERSION    := 0.12.0
PYPDF_VERSION       := 6.7.5
BS4_VERSION         := 4.14.3
LXML_VERSION        := 6.0.2
NLTK_VERSION        := 3.9.3
REQUESTS_VERSION    := 2.32.5

# ── ML training workload versions ──────────────────────────
TRANSFORMERS_VERSION := 5.3.0
ACCELERATE_VERSION   := 1.13.0
PEFT_VERSION         := 0.18.1
DATASETS_VERSION     := 4.6.1
MLFLOW_VERSION       := 3.10.0
ROUGE_VERSION        := 0.1.2
SENTENCEPIECE_VERSION:= 0.2.1

# ── GenAI workload versions ─────────────────────────────────
VLLM_VERSION              := 0.17.0
LANGCHAIN_VERSION         := 1.2.10
LANGCHAIN_COMMUNITY_VERSION := 0.4.1
SENTENCE_TRANSFORMERS_VERSION := 5.2.3
OPENAI_VERSION            := 2.24.0
ANTHROPIC_VERSION         := 0.39.0
FAISS_VERSION             := 1.13.2
CHROMADB_VERSION          := 1.5.2
PSYCOPG2_VERSION          := 2.9.11
PGVECTOR_VERSION          := 0.4.2

# ── LLM training (Ray) workload versions ────────────────────
# (transformers/peft/accelerate/datasets reuse the same pins)
BITSANDBYTES_VERSION := 0.49.2
DEEPSPEED_VERSION    := 0.18.6
FLYTE_RAY_PLUGIN     := 1.16.14
OPENRLHF_VERSION     := 0.9.5

# ── Image release tag ────────────────────────────────────────
# Global default tag. Per-image overrides below take precedence
# when set.  `ml-plat component bump-image <name>` auto-increments
# the per-image tag after a new build.
IMAGE_TAG := 1.1.0

# ── Per-image version overrides ──────────────────────────────
# Convention: IMAGE_TAG_<IMAGE_SHORT_NAME_UPPER> := <semver>
# _resolve_image() checks for the per-image key first, then
# falls back to the global IMAGE_TAG above.
IMAGE_TAG_BASE_CPU     := 1.1.0
IMAGE_TAG_DATA_CPU     := 1.2.0
IMAGE_TAG_ML_GPU       := 1.1.1
IMAGE_TAG_TRAINING_LLM := 1.1.0
IMAGE_TAG_GENAI_GPU    := 1.1.0
IMAGE_TAG_RAY_WORKER   := 1.1.0
IMAGE_TAG_WORKFLOW_CPU := 1.0.0
IMAGE_TAG_WORKFLOW_GPU := 1.0.0
IMAGE_TAG_TEXT2SQL_SERVE := 1.0.0
IMAGE_TAG_BACKEND_API  := 1.0.0
IMAGE_TAG_DASHBOARD    := 1.0.0
IMAGE_TAG_NOTEBOOK_MARIMO_VSCODE := 1.0.0
IMAGE_TAG_EXECUTION_SERVICE := 1.0.0
IMAGE_TAG_REGISTRY_SERVICE  := 1.0.0

# ── ECR registry ─────────────────────────────────────────────
ECR_REGISTRY := 805673386114.dkr.ecr.us-west-2.amazonaws.com
ECR_REPO     := ml-platform
